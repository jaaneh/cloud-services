# Noroff Cloud Services

This repository contains Render.com deploy blueprints.

Since Render.com doesn't support monorepos when using blueprints, each service lives inside its own branch. This avoids having to create a new repository for each service.

- [MinIO]()
- [Monitoring & Logging]()
